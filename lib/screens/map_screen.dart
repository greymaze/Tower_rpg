import 'package:flutter/material.dart';

class MapScreen extends StatelessWidget {
 @override
 Widget build(BuildContext context) {
   return Scaffold(
     appBar: AppBar(title: Text('Carte de la Tour')),
     body: Center(child: Text('Exploration par étage et mini-cartes placeholder')),
   );
 }
}
