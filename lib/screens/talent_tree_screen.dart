import 'package:flutter/material.dart';

class TalentTreeScreen extends StatelessWidget {
 @override
 Widget build(BuildContext context) {
   return Scaffold(
     appBar: AppBar(title: Text('Arbre de talents')),
     body: Center(child: Text('Sélection de talents avec prérequis')),
   );
 }
}
