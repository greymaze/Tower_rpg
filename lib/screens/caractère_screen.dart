import 'package:flutter/material.dart';

class CharacterScreen extends StatelessWidget {
 @override
 Widget build(BuildContext context) {
   return Scaffold(
     appBar: AppBar(title: Text('Personnage')),
     body: Center(child: Text('Création et personnalisation du personnage')),
   );
 }
}
